package org.example.dartgame;

import javafx.scene.control.TextField;
import javafx.stage.Stage;
import javafx.scene.Scene;
import javafx.scene.control.Label;
import javafx.scene.layout.*;



public class createPlayer{
    private Scene first;
    /*
    public PlayerMenu(){

        BorderPane createP = new BorderPane();
        GridPane grid = new GridPane();
        first = new Scene(createP,1300, 800);

        TextField oneTxt =  new TextField();
        TextField twoTxt =  new TextField();
        TextField threeTxt =  new TextField();

        Label one = new Label("Name");
        Label two = new Label("Alter");
        Label three = new Label("Benutzername");

        grid.add(oneTxt, 100, 100);
        grid.add(twoTxt, 100, 100);
        grid.add(threeTxt, 100, 100);

        grid.getColumnConstraints().add(new ColumnConstraints(650));

        grid.getRowConstraints().add(new RowConstraints(50));
        grid.getRowConstraints().add(new RowConstraints(100));
        grid.getRowConstraints().add(new RowConstraints(100));
        grid.getRowConstraints().add(new RowConstraints(50));
        grid.getRowConstraints().add(new RowConstraints(100));
        grid.getRowConstraints().add(new RowConstraints(100));
        grid.getRowConstraints().add(new RowConstraints(50));
        grid.getRowConstraints().add(new RowConstraints(100));
        grid.getRowConstraints().add(new RowConstraints(100));
        grid.getRowConstraints().add(new RowConstraints(50));

        grid.add(one, 0, 1);
        grid.add(oneTxt, 0, 2);
        grid.add(two, 0, 4);
        grid.add(twoTxt, 0, 5);
        grid.add(three, 0, 7);
        grid.add(threeTxt, 0, 8);

        createP.setLeft(grid);


    }

    public static void main(String[] args) {

    }
    public void show(Stage primaryStage){
        primaryStage.setScene(first);
    }

     */
}
